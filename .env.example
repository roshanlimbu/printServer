# Server Configuration
PORT=3000

# Organization Settings
ORG_NAME=Your Organization Name

# Printer Settings
PRINTER_NAME=
PAGE_WIDTH=32

# Print Method: 'auto', 'windows', 'cups', 'file', 'mock'
# 'auto' - Auto-detects platform (Windows uses 'windows', macOS/Linux uses 'cups')
# 'windows' - Use Windows PowerShell printing (Windows only)
# 'cups' - Use CUPS printing (macOS/Linux)
# 'file' - Save to file for testing
# 'mock' - Console output for development
PRINT_METHOD=auto
